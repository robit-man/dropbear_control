# Iteration 11 delivery test plan

Status: `COMPLETE-OFFLINE`

## Source lifecycle

- empty registry is canonical and denial-only;
- exact V1 decisions are revalidated against current inventory;
- human independent accept/reject/revoke/supersede events;
- invalid transition, sequence/time, digest, decision hash and approver deny;
- at most one active runtime-complete decision;
- supersession is atomic and deterministic;
- failed rebuild never changes tracked output.

## Structured graph V2

- exact frame tree and finite rigid transforms;
- alias namespaces and target closure;
- explicit chirality and complete symmetry pairs;
- structured mimic/coupling ratios/domains/singularities;
- physical and simulator closure partition;
- independent/dependent/passive/fixed/simulator DOF reconciliation;
- exact ownership and dependency-state semantics;
- V1 migration remains incomplete;
- synthetic positives never become tracked evidence.

## Projection/API and adapter intake

- lifecycle generation/hash parity across four views;
- revocation invalidates handles and positive synthetic mappings;
- browser redaction;
- TWAI/MCP2515 exact-kind neutrality;
- no-I/O manifest/factory behavior;
- no runtime file modification.

## Regression

- deterministic generators in `--check`;
- schemas Draft 2020-12 strict;
- catalog and trace counts exact;
- full offline gate, web, ESP32 compile and whitespace.

All planned cases are registered in the catalog and complete in the unified
gate. Positive lifecycle, graph and adapter cases use temporary synthetic
fixtures only; no tracked physical evidence was created.
