# Iteration 12 delivery test plan

Status: `VERIFIED-OFFLINE`

## Runtime catalog

- exact 44-model and 53-configuration/source-variant joins;
- source hash and generated digest reproduction;
- model/config/backend exact lookup with no alias/family/default fallback;
- separate acquired STEP, candidate, accepted articulation and runtime asset
  states;
- separate protocol tooling, synthetic plant, sourced plant and physical
  validation states;
- Dropbear lifecycle generation and simulator projection parity;
- redaction of archive, local and evidence paths;
- transactional generation and unknown-file rejection;
- schema/count/hash/claim/substitution mutation denials.

## Simulation session

- all legal and illegal lifecycle transitions;
- fixed virtual tick, no read-driven or wall-clock stepping;
- finite typed command targets, sequence and deadline boundaries;
- reset generation, seed and initial-state digest;
- dense canonical event trace and stable rolling digest;
- snapshot/restore identity and cross-engine mismatch denial;
- deterministic scheduled fault injection and recovery/latched dispositions;
- catalog/source/graph generation revocation before command/read/tick;
- plant, protocol and replay adapter-specific evidence invariants;
- rigid-body, model-fidelity and physical adapter denial;
- reusable cross-backend contract vectors.

## ROS control core

- closed descriptor and interface types;
- exact graph mapping coverage and ordering;
- lifecycle parity with simulation/hardware session;
- absent/stale/faulted state never coerced to zero;
- finite command validation, active-state and lease checks;
- read/write isolation and structured return dispositions;
- generation revocation and backend fault propagation;
- no raw bytes, native CAN identifiers or hidden fallback surface;
- clean import without ROS packages installed.

## Regression

- generated artifacts pass check mode and canonical encoding;
- host/browser parity and browser regression pass;
- test catalog and traceability remain exact;
- complete offline machine gate and ESP32 compile pass;
- no physical evidence or support claim changes.
