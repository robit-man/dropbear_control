# Graph V2 governance intake

The tracked baseline contains no graph submissions or lifecycle events.

- `decisions/` accepts exact V2 graph submission envelopes.
- `events/` accepts globally sequenced accept, reject, revoke or supersede
  events.

Do not place drafts or synthetic fixtures here. A graph event selects only a
canonical graph for downstream projection; it never grants motor support or
physical motion authority.
